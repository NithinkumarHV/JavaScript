// Coercion

console.log("Hello" + " World")
console.log(1 + "2") // 12, JS coerced 1(number) to string "1"


/* 
    Coercion:

    Converting a number from one type to another
    It happens quite a bit in JS, bcz it is dynamically type
*/